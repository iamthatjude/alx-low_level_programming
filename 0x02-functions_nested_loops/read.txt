Google:
gcc manpage
write manpage
=====================

0. _putchar

- A program that prints "_putchar" to the standard output, followed by a new line.
-: create a C program with the .c extension that has the main() function and does the real printing.
-: include the header file main.h
-: in main.h , declare the _putchar function and save
-: create a C file _putchar.c , include the header file main.h and declare the _putchar() function that returns a value


----
1. I sometimes suffer from insomnia. And when I can't fall asleep, I play what I call the alphabet game

- A function that prints the alphabet in lowercase, followed by a new line.
- Prototype: void print_alphabet(void);
- You can only use _putchar twice in your code
-: add a description for this C file, that is, the void function


----
2. 10 x alphabet

- A function that prints the alphabet, in lowercase 10 times, followed by a new line.
-: declare the function in the main.h file first
-: use two while loops (nested while loop) for this task


----
3. islower

- A function that checks for lowercase character. It returns 1 if lowercase and 0 if something else
-: declare the function _islower() in the main.h first
-: write a good description
-: define the function _islower in it's given filename


----
4. isalpha

- A function that checks for lowercase or uppercase character. It returns 1 if lowercase or uppercase and 0 if something else
-: declare the function _isalpha() in the main.h first
-: write a good description
-: define the function _isalpha in it's given filename


----
5. Sign

- A function that prints the sign of a number
- Prototype: int print_sign(int n);
- Returns 1 and prints + if n is greater than zero
- Returns 0 and prints 0 if n is zero
- Returns -1 and prints - if n is less than zero
-: declare the function in the header file first (main.h)
-: write description and define the function in it's given file


----
6. There is no such thing as absolute value in this world. You can only estimate what a thing is worth to you

- Prototype: int _abs(int);
- man abs