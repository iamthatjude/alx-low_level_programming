#include "main.h"
#include <stdio.h>

/**
 * *_strcat - Concatenates two strings
 *
 * @dest: receiver string
 * @src: string to be concatenated with @dest
 *
 * Return: Always 0
*/
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
